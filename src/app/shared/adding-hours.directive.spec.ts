import { AddingHoursDirective } from './adding-hours.directive';

describe('AddingHoursDirective', () => {
  it('should create an instance', () => {
    const directive = new AddingHoursDirective();
    expect(directive).toBeTruthy();
  });
});
